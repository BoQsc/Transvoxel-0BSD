# Stage 06 — interactive sandbox

Human evaluation stage. Run with `RUN_INTERACTIVE.cmd` from the repository root.

This is not part of the production gate. It lets you fly around, switch fields, dig/add material, and save a small session report.
