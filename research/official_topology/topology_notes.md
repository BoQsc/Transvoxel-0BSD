# Topology Notes

The current independent core uses a tetrahedralized transition-cell construction that passes our functional seam/edit proofs.

v30 topology-signature analysis showed that this construction does not naturally collapse to the official 73 transition equivalence classes. That is evidence that the current topology is probably not the official topology family, even though it is functionally consistent under the current proof gate.

Next research questions:

1. What exact equivalence relation produces the official 73 classes?
2. Which transition-cell sample and edge layout is required before class reduction?
3. Which signs/corners are inverted versus rotated in the official reference convention?
4. Can a no-copy generator derive the same topology signatures without reading table values?
5. Can the official-style topology pass the same C/Godot/auto-interaction gate as the independent core?
