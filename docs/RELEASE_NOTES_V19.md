# v19

Reporting polish.

- `ONE_CLICK_RESULT.txt` now lists only report files that actually exist.
- `one_click_report.json` now includes an `interactive_session` summary.
- Upload bundle manifest explains when interactive session data is included.
- Added `docs/REPORT_INTERACTIVE.md`.

This avoids pretending that an interactive sandbox session exists after a normal full proof run.
