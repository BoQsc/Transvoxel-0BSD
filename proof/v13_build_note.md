# v13 note

Adds automatic single-file reporting bundle:

```text
proof/SEND_TO_CHATGPT.zip
runs/run_YYYYMMDD_HHMMSS/SEND_TO_CHATGPT.zip
```

The bundle is the preferred file to drag into ChatGPT after running `RUN.cmd`.
